<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.papb.tapapb">

    <application
        android:allowBackup="true"
        android:icon="@drawable/doa_anak"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DoaAnakActivity" />
        <activity android:name=".DoaDetailActivity" />
        <activity android:name=".PanduanActivity" />
        <activity android:name=".SettingAlarm"></activity>
    </application>

</manifest>
